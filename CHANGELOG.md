# Historial de Versiones

## 1.0.0 - 2026-06-20

- Se preparo separacion de ambientes development, sandbox y production.
- Se creo la base `pos_sandbox` como copia inicial de `pos`.
- Se agrego refresco de sandbox con `npm run sandbox:refresh`.
- Se centralizo la URL del backend en `frontend/src/config.js`.
- Se agrego aviso visual `SANDBOX` y version visible del sistema.
- Se preparo build separado para APK productiva y APK sandbox.
